package main

import "fmt"

func main() {
  // basic types are: bool, string, int and float64
  // var card string = "Ace of spades" or
  // we only use := when declaration a new variable, when reassign just use equals
  card := "Ace of spades"

  fmt.Println(card)
}
